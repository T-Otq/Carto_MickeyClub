function cle shinny

renderplot permet d'afficher un plot

renderDatatable() create an interactive table
renderimage() create an image
renderplot() renvoie un graph
renderUI() permet de réglementer l'accés à des personne ayant les mdp et ID 
renderplotly() renvoie un graph permet en plus de renderplot d'avoir accés à chaque valeur des points et réaliser des zoom sur le graph
